# Geometry v2.4 — start here

Open **GEOMETRY_Lab.html** in a browser to explore the geometry. Choose a witness, add retained coordinates, and watch a collision disappear. Move the timing slider to see the exact mean/variance tradeoff in a synthetic readiness model. The page is fully offline.

Read **GEOMETRY_Working_Master_v2.4.md** for the repaired core, proofs, operational rules, and source links. The main additions are evidence-aware coverage, minimal available-coordinate repairs, metric recovery radius, finite predictive refinement, a uniform path-error bound, and an optimal causal-padding frontier.

Use **GEOMETRY_Corpus_Map.md** to navigate the 52 geometry-titled files and eight focused supporting sources. **GEOMETRY_Search_Index.json** records all 492 distinct broad-search hits and their read depth. Discovery does not mean every item was deeply reviewed. Three pairs have identical normalized extracted text; matching titles alone do not establish duplicates.

**GEOMETRY_Proofs_and_Review.md** contains the longer mathematical, service, and attachment audits. **GEOMETRY_Working_Master_v2.3_source.md** preserves the supplied starting packet. The review restores qualifications that the packet had lost; source experiments and historical statuses remain separate.

For executable work, **geometry_engine.py** provides exact finite collision certificates, factor maps, canonical refinement, deterministic predictive partitions, rational strong-lumpability partitions, and inclusion-minimal/minimum-cardinality coordinate repairs. It uses Python's standard library. Running `python geometry_engine.py` prints synthetic examples; running `python geometry_tests.py` verifies the implementation and refreshes the adjacent **geometry_verification.json** receipt.

The recorded engine run passed 16 test methods and 4,464 exhaustive small-table cases. The lab passed 1,602 runtime assertions and desktop/mobile visual inspection. **geometry_padding_checks.json** holds two exact illustrative padding calculations. These are constructed mathematical checks, not physical or human experiments.

All 60 reviewed Drive sources have local text snapshots in the task's work/sources folder; the corpus map records their paths and hashes. Those scratch snapshots are not duplicated into this compact release archive. Original source links remain in the documents.

Google Docs publication is a separate final step. The content is finished locally. The app's automatic approval review requires explicit approval before writing the synthesis to the newly created private Google Doc. No existing source document has been edited.

**GEOMETRY_Release_Manifest.json** gives sizes and SHA-256 hashes of the packaged deliverables. **GEOMETRY_v2.4_Complete_Pack.zip** contains the release files together.
